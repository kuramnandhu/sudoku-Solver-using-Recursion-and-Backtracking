# Sudoku Solver (Flask + Python)

This project is a **web-based Sudoku solver** built using:
- Python
- Flask
- HTML, CSS

### 🚀 How to Run Locally
```bash
pip install -r requirements.txt
python app.py

